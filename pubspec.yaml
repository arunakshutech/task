name: task
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.12.1
  provider: ^6.1.2
  firebase_auth: ^5.5.1
  sqflite: ^2.4.2
  fluttertoast: ^8.2.12
  google_fonts: ^6.2.1
  cloud_firestore: ^5.6.5
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
